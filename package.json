{
  "name": "web-to-exe",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",  // 启动 Electron 应用
    "build": "electron-builder"  // 定义 build 脚本，使用 electron-builder 打包应用
  },
  "devDependencies": {
    "electron": "^24.0.0",
    "electron-builder": "^23.0.0"
  }
}