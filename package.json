{
  "name": "web-to-exe",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . WebToExe --platform=win32 --arch=x64 --out=release-build --overwrite"
  },
  "devDependencies": {
    "electron": "^24.0.0",
    "electron-packager": "^15.0.0"
  }
}